# Quick test config for timing verification
models:
  judge_type: single
  randomize_argument_order: true  # Mitigate LLM positional bias
  debaters:
    - name: "Alpha"
      model: "qwen2.5:1.5b"
    - name: "Beta"
      model: "qwen2.5:1.5b"
  judges:
    - model: "qwen2.5:1.5b"

economy:
  initial_balance: 200
  max_debt: 50
  tokens_per_round: 100

rounds:
  max_iterations: 5
  topic: "Should AI be regulated?"
